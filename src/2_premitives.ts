let username: string = "john_doe";
let age: number = 30;
let isAdmin: boolean = false;

let big: bigint = 9007199254741991n;
let symbol: symbol = Symbol("uniqueIdentifier");

function greet(name: string): string {
    return `Hello, ${name}!`;
}
